#!/usr/bin/node
// a script that:
// prints 3 lines using an array of string and loop

const row = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let j = 0; j < row.length; j++)
{
  console.log(row[j]);
}
